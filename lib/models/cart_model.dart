class CartModel 
{
    var id;
    var name;
    var image;
    var price;
    var quantity;
    var product_id;

    CartModel(this.id, this.name, this.image, this.price, this.quantity, this.product_id);
}